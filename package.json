{
  "name": "webgpu-lib",
  "version": "0.0.1",
  "main": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "copy-shader": "copyfiles -u 1 ./src/renderers/shaders/**/*.wgsl ./build/",
    "build": "tsc && yarn copy-shader",
    "clean-build": "rimraf ../build && yarn build"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.6",
    "copyfiles": "^2.4.1",
    "gl-matrix": "^3.3.0",
    "raw-loader": "^4.0.2",
    "rimraf": "^3.0.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
